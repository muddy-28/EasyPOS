import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    width: 46,
    height: 46,
    justifyContent: 'center',
    alignItems: 'center',
  },
  icon: {
    resizeMode: 'center',
  }
})
